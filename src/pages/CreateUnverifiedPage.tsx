import { MainLayout } from '@widgets/layout/MainLayout';
import { UnverifiedContent } from '@features/unverified/ui/UnverifiedContent.tsx';

export const CreateUnverifiedPage = () => {
    return (
        <MainLayout>
            <UnverifiedContent />
        </MainLayout>
    );
};
